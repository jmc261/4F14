#include <iostream>
#include <thread>
#include <vector>
#include <algorithm>

int main() {
    std::vector<int> vect;
    vect.push_back(3);
    vect.push_back(2);
    vect.push_back(1);

    std::cout<<vect[0]<<std::endl;

};
